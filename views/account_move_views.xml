<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_out_invoice_tree_filter_list" model="ir.ui.view">
        <field name="name">account.out.invoice.tree.filter_list</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="js_class">filter_list</attribute>
            </xpath>
            <xpath expr="//list" position="inside">
                <field name="create_date" optional="hide"/>
                <field name="write_date" optional="hide"/>
            </xpath>
        </field>
    </record>

    <record id="account.action_move_out_invoice_type" model="ir.actions.act_window">
        <field name="view_mode">list,kanban,form</field>
        <field name="view_id" ref="view_out_invoice_tree_filter_list"/>
    </record>
</odoo>
